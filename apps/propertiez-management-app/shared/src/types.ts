// App-specific types for propertiez-management-app
import type { User } from '@portfolio/types';

export interface AppUser extends User {
  // Add app-specific user properties
}

// Add more app-specific types here
